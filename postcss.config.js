export default {
    plugins: {
        // autoprefixer is handled by tailwind/vite usually, but can keep it if needed. 
        // For now, let's clear it to avoid the 'tailwindcss' plugin error.
    },
}
